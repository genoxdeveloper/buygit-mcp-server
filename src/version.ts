// Bumped manually on every npm publish. tsup folds this into the bundle
// so the running server can identify its own version in logs and in the
// MCP `initialize` handshake.
export const VERSION = '0.9.0';
export const NAME = '@buygit/mcp-server';
export const HOMEPAGE = 'https://buygit.com/mcp';
